// setInterval irá rodar uma função N vezes

const timeOut = 1000;

const checking = () => console.log('checking !');

setInterval(checking, timeOut); // Aqui está sendo usada a funçaõ setInterval que serve para rodar um comando após um intervalo determinado de tempo..... a definição está sendo passada pelos parametros checking que é a funçao, e o timeOut queé o tempo determinado.